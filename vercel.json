{
  "buildCommand": "node ./scripts/set-env.js && npx nx build admin --configuration=production",
  "installCommand": "npm install --include=dev",
  "outputDirectory": "dist/apps/admin/browser",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
