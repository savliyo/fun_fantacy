const {
  APP_NAME = 'maximus',
  NODE_ENV = 'development',
  PORT = 3000,
  ENCRYPT = false,
  NODE_HOST = 'localhost',
  REDIS_HOST = 'localhost',
  REDIS_PORT = '6379',
  DB_NAME = 'fun_fantacy',
  DB_HOST = 'localhost',
  DB_PORT = '5432',
  DB_USERNAME = 'postgres',
  DB_PASSWORD = 'postgres',
  DB_SSL = 'false',
  DEFAULT_IMAGE_QUALITY_REDUCTION,
  SENDER_EMAIL_HOST = '',
  SENDER_EMAIL_PORT,
  SENDER_EMAIL_ID = '',
  SENDER_EMAIL_PASSWORD = '',
  JWT_SECRET = 'Fun_Fantacy',
  PAYMENT_API,
  FIREBASE_PRIVATE_KEY_ID = "6cc9f40ad731eeb58f954f32c8d082923b30eaf2",
  RAZORPAY_KEY,
  RAZORPAY_SECRET,
} = process.env;

export const APP_SETTINGS = {
  SENDER_EMAIL_PORT: parseInt(SENDER_EMAIL_PORT || '465'),
  SENDER_EMAIL_HOST,
  SENDER_EMAIL_ID,
  SENDER_EMAIL_PASSWORD,
  NODE_ENV,
  PORT,
  DB_NAME,
  DB_HOST,
  DB_PORT: parseInt(DB_PORT),
  DB_USERNAME,
  DB_PASSWORD,
  DB_SSL: DB_SSL === 'true',
  ENCRYPT,
  NODE_HOST,
  APP_NAME,
  DEFAULT_IMAGE_QUALITY_REDUCTION: parseInt(
    DEFAULT_IMAGE_QUALITY_REDUCTION || '80'
  ),
  IS_DEVELOPMENT: NODE_ENV === 'development',
  JWT_SECRET,
  PAYMENT_API,
  FIREBASE_PRIVATE_KEY_ID,
  RAZORPAY_KEY,
  RAZORPAY_SECRET,
  REDIS_HOST,
  REDIS_PORT,
};
